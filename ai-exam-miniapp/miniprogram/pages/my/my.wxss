.my-page {
  background: radial-gradient(circle at 72% 8%, #f0efff 0, #fbfcff 32%, #ffffff 100%);
}

.my-container {
  padding-top: 34rpx;
}

.user-box {
  height: 170rpx;
  display: flex;
  align-items: center;
}

.avatar {
  width: 118rpx;
  height: 118rpx;
  border-radius: 50%;
  border: 8rpx solid #fff;
  box-shadow: 0 12rpx 28rpx rgba(110, 96, 190, .16);
  margin-right: 40rpx;
  background: #ddd8ec;
}

.nickname {
  color: #171923;
  font-size: var(--font-hero);
  font-weight: var(--weight-bold);
}

.uid {
  color: #7b8499;
  font-size: var(--font-body);
  line-height: 40rpx;
  margin-top: 18rpx;
  max-width: 520rpx;
}

.member-card {
  min-height: 218rpx;
  margin-top: 30rpx;
  border-radius: 18rpx;
  border: 1rpx solid #f2bc5f;
  background: linear-gradient(135deg, #fff9ed 0%, #fffefb 62%, #fff3d5 100%);
  display: flex;
  align-items: flex-start;
  padding: 36rpx 32rpx 26rpx;
  position: relative;
  overflow: hidden;
}

.member-card::after {
  content: '';
  position: absolute;
  right: 8rpx;
  bottom: -30rpx;
  width: 180rpx;
  height: 140rpx;
  background: rgba(242, 174, 51, .12);
  border-radius: 80rpx 80rpx 0 0;
}

.member-crown {
  width: 82rpx;
  height: 82rpx;
  margin: 40rpx 32rpx 0 0;
  z-index: 1;
}

.member-info {
  flex: 1;
  z-index: 1;
}

.member-title {
  color: #171923;
  font-size: var(--font-display);
  font-weight: var(--weight-bold);
}

.member-date,
.member-tip {
  color: #9a762e;
  font-size: var(--font-sm);
  line-height: 36rpx;
  margin-top: 12rpx;
  width: 280rpx;
}

.check-btn {
  position: absolute;
  right: 34rpx;
  top: 76rpx;
  width: 190rpx;
  height: 66rpx;
  border-radius: 14rpx;
  border: 1rpx solid #efa63a;
  color: #d87900;
  font-size: var(--font-body);
  font-weight: var(--weight-bold);
  z-index: 1;
}

.points-card {
  height: 154rpx;
  margin-top: 28rpx;
  border-radius: 18rpx;
  background: linear-gradient(135deg, #6f59ff 0%, #5548ef 100%);
  padding: 28rpx 38rpx;
  color: #fff;
  display: flex;
  justify-content: space-between;
  align-items: center;
  overflow: hidden;
  position: relative;
}

.points-label {
  font-size: var(--font-body);
  font-weight: var(--weight-bold);
}

.points-num {
  font-size: var(--font-display);
  line-height: 1;
  font-weight: var(--weight-bold);
  margin-top: 18rpx;
}

.points-num text {
  font-size: var(--font-lg);
}

.buy-btn {
  width: 286rpx;
  height: 70rpx;
  border-radius: 14rpx;
  background: #fff;
  color: #5a4bf4;
  font-size: var(--font-lg);
  font-weight: var(--weight-bold);
}

.share-card {
  margin-top: 28rpx;
  padding: 34rpx 32rpx;
}

.share-title {
  font-size: var(--font-section);
  font-weight: var(--weight-bold);
  color: #171923;
}

.share-desc {
  color: #737c91;
  font-size: var(--font-body);
  line-height: 44rpx;
  margin: 18rpx 0 30rpx;
}

.share-btn {
  width: 100%;
  height: 78rpx;
  border-radius: 14rpx;
  background: linear-gradient(135deg, #6b55ff, #5548ef);
  color: #fff;
  font-size: var(--font-lg);
  font-weight: var(--weight-bold);
  display: flex;
  align-items: center;
  justify-content: center;
}

.share-btn.poster {
  margin-top: 18rpx;
  background: linear-gradient(135deg, #f7f6ff, #efecff);
  color: #5a4bf4;
  border: 1rpx solid #ddd8ff;
  box-shadow: none;
}

.share-btn text {
  margin-right: 14rpx;
}

.invite-link {
  height: 74rpx;
  border-radius: 14rpx;
  border: 2rpx dashed #aaa2ff;
  color: #5a4bf4;
  font-size: var(--font-body);
  font-weight: var(--weight-bold);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 20rpx;
}

.invite-link text {
  margin-right: 12rpx;
}

.share-status {
  color: #5a4bf4;
  margin-top: 16rpx;
  text-align: center;
  font-size: var(--font-sm);
}

.list-card {
  margin-top: 28rpx;
  padding: 0 34rpx;
}

.list-item {
  height: 116rpx;
  display: flex;
  align-items: center;
  color: #171923;
  font-size: var(--font-section);
  font-weight: var(--weight-bold);
}

.line {
  height: 1rpx;
  background: #edf0f5;
  margin-left: 88rpx;
}

.list-icon {
  width: 56rpx;
  height: 56rpx;
  border-radius: 12rpx;
  background: linear-gradient(135deg, #6b55ff, #5046ea);
  margin-right: 34rpx;
  position: relative;
}

.list-icon.doc::after {
  content: '';
  position: absolute;
  left: 19rpx;
  top: 14rpx;
  width: 18rpx;
  height: 26rpx;
  border-radius: 3rpx;
  background: #fff;
}

.list-icon.bag::after {
  content: '';
  position: absolute;
  left: 15rpx;
  top: 18rpx;
  width: 26rpx;
  height: 23rpx;
  border-radius: 6rpx;
  border: 4rpx solid #fff;
  border-top: 0;
}

.chev {
  margin-left: auto;
  color: #7f8798;
  font-size: var(--font-number);
  font-weight: var(--weight-regular);
}

.share-mask {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  background: rgba(31, 38, 58, .58);
  z-index: 80;
  display: flex;
  align-items: center;
  justify-content: center;
}

.share-dialog {
  width: 562rpx;
  min-height: 600rpx;
  border-radius: 24rpx;
  background: #fff;
  overflow: hidden;
  box-shadow: 0 28rpx 80rpx rgba(0,0,0,.18);
}

.send-title {
  padding: 44rpx 42rpx 24rpx;
  color: #171923;
  font-size: var(--font-title);
  font-weight: var(--weight-bold);
}

.friend-row {
  display: flex;
  align-items: center;
  padding: 0 42rpx 30rpx;
}

.friend-avatar {
  width: 84rpx;
  height: 84rpx;
  background: linear-gradient(135deg, #eeeeee, #d9d9d9);
  margin-right: 24rpx;
  position: relative;
}

.friend-avatar::after {
  content: '';
  position: absolute;
  left: 24rpx;
  top: 20rpx;
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  background: #fff;
}

.friend-name {
  color: #171923;
  font-size: var(--font-lg);
  font-weight: var(--weight-bold);
}

.mini-card {
  width: 230rpx;
  height: 242rpx;
  border-radius: 8rpx;
  border: 1rpx solid #e1e4ec;
  margin: 0 auto 34rpx;
  padding: 22rpx;
}

.mini-title {
  text-align: center;
  color: #171923;
  font-size: var(--font-body);
  font-weight: var(--weight-bold);
  margin-bottom: 18rpx;
}

.mini-shot {
  width: 120rpx;
  height: 120rpx;
  display: block;
  margin: 0 auto;
  border-radius: 4rpx;
}

.mini-type {
  color: #7a8396;
  font-size: var(--font-body);
  font-weight: var(--weight-bold);
  margin-top: 16rpx;
}

.dialog-actions {
  height: 96rpx;
  border-top: 1rpx solid #e7e9ef;
  display: flex;
}

.dialog-actions view {
  width: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #171923;
  font-size: var(--font-lg);
  font-weight: var(--weight-bold);
}

.dialog-actions .send {
  color: #5a4bf4;
  border-left: 1rpx solid #e7e9ef;
}

.my-page {
  background:
    radial-gradient(circle at 20% 8%, rgba(99, 80, 244, .07), transparent 31%),
    radial-gradient(circle at 88% 16%, rgba(255, 221, 145, .12), transparent 24%),
    linear-gradient(180deg, #fbfcff 0%, #ffffff 64%);
}

.my-container {
  padding-top: 22rpx;
  padding-left: 38rpx;
  padding-right: 38rpx;
}

.user-box {
  height: 196rpx;
  align-items: center;
}

.avatar {
  width: 142rpx;
  height: 142rpx;
  border: 9rpx solid #fff;
  box-shadow: 0 18rpx 42rpx rgba(102, 91, 170, .16);
  margin-right: 42rpx;
}

.nickname {
  font-size: var(--font-display);
  line-height: 58rpx;
}

.uid {
  margin-top: 16rpx;
  font-size: var(--font-body);
  line-height: 36rpx;
  color: #778196;
  max-width: 500rpx;
}

.member-card {
  min-height: 224rpx;
  margin-top: 24rpx;
  border-radius: 22rpx;
  border-color: #f0bd62;
  background:
    linear-gradient(135deg, rgba(255,255,255,.78) 0%, rgba(255,250,239,.94) 48%, rgba(255,245,221,.9) 100%);
  padding: 38rpx 34rpx 28rpx;
}

.member-card::before {
  content: '';
  position: absolute;
  right: 96rpx;
  top: 48rpx;
  width: 18rpx;
  height: 18rpx;
  border-radius: 50%;
  background: rgba(245, 190, 76, .22);
  z-index: 0;
}

.member-card::after {
  right: -18rpx;
  bottom: -24rpx;
  width: 190rpx;
  height: 160rpx;
  border-radius: 96rpx 96rpx 0 0;
  background:
    radial-gradient(circle at 50% 18%, rgba(242, 174, 51, .22) 0 10rpx, transparent 12rpx),
    linear-gradient(135deg, rgba(242, 174, 51, .12), rgba(242, 174, 51, .04));
  opacity: .10;
}

.member-crown {
  width: 88rpx;
  height: 88rpx;
  margin: 42rpx 34rpx 0 0;
}

.member-title {
  font-size: var(--font-display);
  line-height: 58rpx;
}

.member-date,
.member-tip {
  width: 330rpx;
  font-size: var(--font-body);
  line-height: 42rpx;
  margin-top: 10rpx;
  color: #96722d;
}

.check-btn {
  right: 34rpx;
  top: 82rpx;
  width: 244rpx;
  height: 74rpx;
  border-radius: 16rpx;
  font-size: var(--font-lg);
  background: rgba(255,255,255,.54);
}

.points-card {
  height: 154rpx;
  margin-top: 30rpx;
  border-radius: 20rpx;
  padding: 26rpx 40rpx;
  background:
    radial-gradient(circle at 49% 112%, rgba(255,255,255,.13), transparent 30%),
    radial-gradient(circle at 94% 80%, rgba(255,255,255,.16), transparent 22%),
    linear-gradient(135deg, #725cff 0%, #5849f0 100%);
  box-shadow: 0 20rpx 44rpx rgba(84, 71, 238, .18);
}

.points-card::before,
.points-card::after {
  content: '';
  position: absolute;
  border-radius: 50%;
  border: 2rpx solid rgba(255, 255, 255, .16);
}

.points-card::before {
  right: 54rpx;
  bottom: -24rpx;
  width: 112rpx;
  height: 52rpx;
  transform: rotate(-18deg);
}

.points-card::after {
  right: 18rpx;
  bottom: -18rpx;
  width: 102rpx;
  height: 46rpx;
  transform: rotate(20deg);
}

.points-label {
  font-size: var(--font-body);
}

.points-num {
  font-size: var(--font-price);
  margin-top: 16rpx;
}

.buy-btn {
  width: 312rpx;
  height: 78rpx;
  border-radius: 16rpx;
  font-size: var(--font-lg);
  box-shadow: 0 12rpx 28rpx rgba(31, 28, 90, .12);
}

.share-card {
  margin-top: 34rpx;
  padding: 36rpx 42rpx 34rpx;
  border-radius: 22rpx;
}

.share-title {
  font-size: var(--font-section);
}

.share-desc {
  font-size: var(--font-body);
  line-height: 46rpx;
  margin: 22rpx 0 30rpx;
}

.share-btn {
  height: 84rpx;
  border-radius: 16rpx;
  font-size: var(--font-lg);
  position: relative;
}

.share-btn.reward::before {
  content: '↗';
  margin-right: 16rpx;
  font-size: var(--font-section);
  line-height: 1;
}

.share-btn.poster::before {
  content: '◎';
  margin-right: 14rpx;
  font-size: var(--font-body);
}

.share-btn text,
.invite-link text {
  display: none;
}

.invite-link {
  height: 80rpx;
  border-radius: 16rpx;
  border-color: #aaa2ff;
  font-size: var(--font-lg);
  margin-top: 22rpx;
}

.invite-link::before {
  content: '↗';
  margin-right: 16rpx;
  font-size: var(--font-title);
  transform: rotate(45deg);
}

.list-card {
  margin-top: 34rpx;
  padding: 0 36rpx;
  border-radius: 22rpx;
}

.list-item {
  height: 126rpx;
  font-size: var(--font-section);
}

.list-icon {
  width: 62rpx;
  height: 62rpx;
  border-radius: 14rpx;
  margin-right: 34rpx;
  box-shadow: inset 0 -8rpx 14rpx rgba(39, 31, 160, .16);
}

.line {
  margin-left: 96rpx;
}

.chev {
  font-size: var(--font-number);
  color: #7f8798;
}

.share-card {
  margin-top: 34rpx;
  padding: 34rpx 34rpx 30rpx;
  border-radius: 24rpx;
  background:
    radial-gradient(circle at 96% 0%, rgba(105, 86, 244, .13), transparent 34%),
    linear-gradient(180deg, #ffffff 0%, #fbfbff 100%);
  border-color: #e7e9f6;
  box-shadow: 0 18rpx 48rpx rgba(44, 55, 96, .07);
  overflow: hidden;
  position: relative;
}

.share-card::before {
  content: '';
  position: absolute;
  right: -54rpx;
  top: -74rpx;
  width: 184rpx;
  height: 184rpx;
  border-radius: 50%;
  background: rgba(103, 84, 244, .08);
}

.share-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  position: relative;
  z-index: 1;
}

.share-eyebrow {
  color: #6a5af4;
  font-size: var(--font-sm);
  line-height: 30rpx;
  font-weight: var(--weight-bold);
  margin-bottom: 8rpx;
}

.share-title {
  color: #171923;
  font-size: var(--font-page-title);
  line-height: 48rpx;
  font-weight: var(--weight-bold);
}

.share-badge {
  height: 48rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  background: #fff5db;
  color: #d67900;
  border: 1rpx solid #ffe1a8;
  font-size: var(--font-xs);
  font-weight: var(--weight-bold);
  display: flex;
  align-items: center;
}

.share-desc {
  color: #778196;
  font-size: var(--font-md);
  line-height: 40rpx;
  margin: 18rpx 0 24rpx;
  position: relative;
  z-index: 1;
}

.reward-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 18rpx;
  margin-bottom: 22rpx;
  position: relative;
  z-index: 1;
}

.reward-item {
  min-height: 108rpx;
  border-radius: 18rpx;
  background: #f6f5ff;
  border: 1rpx solid #ebe8ff;
  padding: 20rpx 22rpx;
}

.reward-num {
  color: #5a4bf4;
  font-size: var(--font-hero);
  line-height: 46rpx;
  font-weight: var(--weight-bold);
}

.reward-copy {
  color: #6f7890;
  font-size: var(--font-sm);
  line-height: 30rpx;
  margin-top: 6rpx;
}

.share-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18rpx;
  position: relative;
  z-index: 1;
}

.share-btn {
  height: 78rpx;
  border-radius: 18rpx;
  font-size: var(--font-body);
  font-weight: var(--weight-bold);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: none;
}

.share-btn.reward {
  background: linear-gradient(135deg, #6d58ff 0%, #5146ee 100%);
  color: #fff;
  box-shadow: 0 14rpx 28rpx rgba(83, 69, 238, .22);
}

.share-btn.reward::before {
  content: '↗';
  margin-right: 12rpx;
  font-size: var(--font-lg);
  line-height: 1;
}

.share-btn.poster {
  margin-top: 0;
  background: #ffffff;
  color: #5a4bf4;
  border: 1rpx solid #ddd9ff;
}

.share-btn.poster::before {
  content: '◎';
  margin-right: 10rpx;
  font-size: var(--font-sm);
}

.invite-link {
  height: 74rpx;
  margin-top: 18rpx;
  border-radius: 18rpx;
  border: 2rpx dashed #b9b1ff;
  color: #5a4bf4;
  background: rgba(255, 255, 255, .7);
  font-size: var(--font-body);
  font-weight: var(--weight-bold);
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  z-index: 1;
}

.invite-link::before {
  content: '↗';
  margin-right: 12rpx;
  font-size: var(--font-body);
  transform: rotate(45deg);
}

.share-status {
  margin-top: 16rpx;
  color: #5a4bf4;
  font-size: var(--font-sm);
  line-height: 32rpx;
  text-align: center;
  position: relative;
  z-index: 1;
}

.my-container {
  padding-top: 20rpx;
}

.member-card {
  min-height: 182rpx;
  border-radius: 22rpx;
}

.member-title {
  font-size: var(--font-hero);
}

.member-date {
  font-size: var(--font-body);
}

.points-card {
  height: 182rpx;
  padding-left: 172rpx;
}

.points-card::before {
  left: 34rpx;
  top: 34rpx;
  width: 104rpx;
  height: 104rpx;
  border: 0;
  background: radial-gradient(circle at 50% 44%, #d9d3ff 0 19rpx, #8774ff 20rpx 48rpx, transparent 49rpx);
  box-shadow: 0 12rpx 26rpx rgba(49, 39, 180, .18);
}

.share-card {
  min-height: 448rpx;
}

.share-title {
  font-size: var(--font-display);
}

.reward-item {
  min-height: 92rpx;
  text-align: center;
  padding: 16rpx 18rpx;
}

.reward-num {
  font-size: var(--font-page-title);
}

.share-mask {
  background: rgba(16, 20, 34, .58);
}

.share-dialog {
  width: 506rpx;
  min-height: 636rpx;
  border-radius: 24rpx;
}

.send-title {
  padding: 36rpx 42rpx 18rpx;
  font-size: var(--font-body);
}

.friend-row {
  padding-bottom: 24rpx;
}

.friend-avatar {
  width: 70rpx;
  height: 70rpx;
  border-radius: 50%;
  background: #dcd7ff;
}

.mini-card {
  width: 240rpx;
  height: 322rpx;
  border-radius: 10rpx;
  padding: 18rpx;
}

.mini-title {
  font-size: var(--font-sm);
  margin-bottom: 14rpx;
}

.mini-shot {
  width: 184rpx;
  height: 184rpx;
  border-radius: 8rpx;
}

.mini-type {
  font-size: var(--font-sm);
  margin-top: 18rpx;
}

.dialog-actions {
  height: 88rpx;
}

.my-container {
  padding-top: 12rpx;
  padding-left: 46rpx;
  padding-right: 46rpx;
}

.user-box {
  height: 150rpx;
  margin-bottom: 22rpx;
}

.avatar {
  width: 128rpx;
  height: 128rpx;
  margin-right: 30rpx;
}

.nickname {
  font-size: var(--font-page-title);
  line-height: 48rpx;
}

.uid {
  margin-top: 10rpx;
  font-size: var(--font-sm);
  line-height: 32rpx;
  white-space: nowrap;
}

.member-card,
.points-card,
.share-card,
.list-card {
  border-radius: 22rpx;
}

.member-card {
  min-height: 176rpx;
  padding: 34rpx 34rpx 26rpx;
  border: 0;
  background: #fff5dc;
  overflow: hidden;
}

.member-bg {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  width: 100%;
  height: 100%;
  opacity: .98;
  z-index: 0;
}

.member-info,
.check-btn {
  position: relative;
  z-index: 1;
}

.member-title {
  color: #7b5017;
  text-transform: uppercase;
}

.member-date,
.member-tip {
  color: #895f22;
}

.member-tip {
  display: none;
}

.check-btn {
  background: rgba(255, 255, 255, .78);
}

.points-card {
  padding-left: 196rpx;
}

.points-card > view {
  position: relative;
  z-index: 2;
  min-width: 176rpx;
}

.points-card .buy-btn {
  position: relative;
  z-index: 2;
}

.points-card .points-num {
  white-space: nowrap;
  line-height: 1;
}

.points-card .points-num text {
  display: inline;
}

.points-visual {
  position: absolute;
  left: 28rpx;
  top: 24rpx;
  width: 142rpx;
  height: 142rpx;
  z-index: 1;
}

.points-card::before {
  display: none;
}

.points-card {
  height: 154rpx;
  padding: 0 36rpx 0 28rpx;
  display: flex;
  align-items: center;
  gap: 26rpx;
}

.points-visual {
  position: static;
  flex: 0 0 112rpx;
  width: 112rpx;
  height: 112rpx;
  border-radius: 20rpx;
  z-index: 2;
}

.points-copy {
  position: relative;
  z-index: 2;
  flex: 1 1 auto;
  min-width: 0;
}

.points-card > .points-copy {
  min-width: 0;
}

.points-label {
  font-size: var(--font-lg);
  line-height: 36rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
}

.points-num {
  display: flex;
  align-items: baseline;
  gap: 12rpx;
  margin-top: 12rpx;
  font-size: var(--font-number);
  line-height: 58rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
  white-space: nowrap;
}

.points-num text {
  display: inline-block;
  font-size: var(--font-body);
  line-height: 32rpx;
  font-weight: var(--weight-bold);
}

.points-card .buy-btn {
  position: relative;
  z-index: 2;
  flex: 0 0 218rpx;
  width: 218rpx;
  height: 72rpx;
  margin: 0;
  border-radius: 18rpx;
  font-size: var(--font-lg);
  line-height: 72rpx;
  box-shadow: 0 10rpx 24rpx rgba(31, 28, 90, .12);
}

.share-card {
  padding-right: 238rpx;
  min-height: 420rpx;
}

.reward-visual {
  position: absolute;
  right: -36rpx;
  top: 42rpx;
  width: 290rpx;
  height: 250rpx;
  opacity: .96;
  z-index: 0;
}

.share-head,
.share-desc,
.reward-grid,
.share-actions,
.invite-link,
.share-status {
  position: relative;
  z-index: 1;
}

.share-actions {
  grid-template-columns: 1fr;
}

.member-card {
  min-height: 168rpx;
  margin-top: 22rpx;
  padding: 28rpx 28rpx 28rpx 34rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-radius: 24rpx;
  border: 1rpx solid rgba(218, 162, 62, .38);
  background:
    linear-gradient(100deg, rgba(255, 255, 255, .92) 0%, rgba(255, 247, 225, .88) 42%, rgba(255, 232, 184, .52) 100%),
    linear-gradient(135deg, #fff7e7 0%, #f6d590 56%, #d99b39 100%);
  box-shadow: 0 14rpx 34rpx rgba(142, 91, 24, .10);
  overflow: hidden;
}

.member-card::before {
  content: '';
  position: absolute;
  left: 0;
  top: 26rpx;
  width: 6rpx;
  height: 116rpx;
  border-radius: 0 999rpx 999rpx 0;
  background: linear-gradient(180deg, #f3cf85, #b87319);
  z-index: 2;
}

.member-card::after {
  content: '';
  position: absolute;
  right: -24rpx;
  bottom: -30rpx;
  width: 170rpx;
  height: 90rpx;
  border: 2rpx solid rgba(180, 118, 32, .16);
  border-radius: 50%;
  background: transparent;
  opacity: 1;
  z-index: 1;
}

.member-bg {
  left: auto;
  right: -24rpx;
  top: -46rpx;
  bottom: auto;
  width: 420rpx;
  height: 236rpx;
  opacity: .42;
  z-index: 0;
}

.member-info {
  flex: 1 1 auto;
  min-width: 0;
  padding-left: 18rpx;
  padding-right: 28rpx;
  position: relative;
  z-index: 2;
}

.member-title {
  color: #6f430d;
  font-size: var(--font-hero);
  line-height: 46rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
  text-transform: none;
}

.member-date,
.member-tip {
  width: auto;
  max-width: 390rpx;
  margin-top: 14rpx;
  color: #8b6628;
  font-size: var(--font-sm);
  line-height: 32rpx;
  font-weight: var(--weight-semibold);
  letter-spacing: 0;
}

.check-btn {
  position: relative;
  right: auto;
  top: auto;
  flex: 0 0 214rpx;
  width: 214rpx;
  height: 68rpx;
  margin: 0;
  border-radius: 18rpx;
  background: rgba(255, 255, 255, .86);
  color: #b96f16;
  font-size: var(--font-body);
  line-height: 68rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
  box-shadow: 0 10rpx 24rpx rgba(123, 76, 18, .12);
  z-index: 2;
}

.check-btn::after {
  border: 0;
}

.share-card {
  min-height: 0;
  margin-top: 30rpx;
  padding: 34rpx 34rpx 32rpx;
  border-radius: 26rpx;
  border: 1rpx solid #e7e9f6;
  background:
    radial-gradient(circle at 90% 6%, rgba(104, 86, 244, .10), transparent 24%),
    linear-gradient(180deg, #ffffff 0%, #fbfbff 100%);
  box-shadow: 0 18rpx 46rpx rgba(42, 52, 90, .07);
  overflow: hidden;
}

.share-card::before {
  content: '';
  position: absolute;
  right: -80rpx;
  top: -70rpx;
  width: 240rpx;
  height: 240rpx;
  border-radius: 50%;
  background: rgba(104, 86, 244, .08);
  z-index: 0;
}

.reward-visual {
  right: 12rpx;
  top: 56rpx;
  width: 210rpx;
  height: 176rpx;
  opacity: .96;
  z-index: 1;
}

.share-head {
  align-items: flex-start;
  padding-right: 190rpx;
}

.share-eyebrow {
  margin-bottom: 8rpx;
  color: #6a5af4;
  font-size: var(--font-sm);
  line-height: 30rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
}

.share-title {
  color: #171923;
  font-size: var(--font-display);
  line-height: 54rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
}

.share-badge {
  position: absolute;
  right: 0;
  top: 4rpx;
  height: 48rpx;
  padding: 0 20rpx;
  border-radius: 999rpx;
  background: #fff7e8;
  border: 1rpx solid #ffd99a;
  color: #c97813;
  font-size: var(--font-xs);
  line-height: 48rpx;
  font-weight: var(--weight-bold);
}

.share-desc {
  width: 410rpx;
  margin: 20rpx 0 26rpx;
  color: #7b8499;
  font-size: var(--font-md);
  line-height: 42rpx;
  font-weight: var(--weight-semibold);
}

.reward-grid {
  width: 410rpx;
  gap: 16rpx;
  margin-bottom: 26rpx;
}

.reward-item {
  min-height: 104rpx;
  padding: 18rpx 16rpx;
  border-radius: 18rpx;
  background: #f7f6ff;
  border: 1rpx solid #e6e3ff;
  text-align: center;
}

.reward-num {
  color: #5b4bf4;
  font-size: var(--font-page-title);
  line-height: 42rpx;
  font-weight: var(--weight-bold);
}

.reward-copy {
  margin-top: 8rpx;
  color: #727b91;
  font-size: var(--font-xs);
  line-height: 28rpx;
  font-weight: var(--weight-semibold);
}

.share-actions {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18rpx;
}

.share-btn {
  height: 76rpx;
  border-radius: 18rpx;
  font-size: var(--font-body);
  line-height: 76rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
}

.share-btn.reward {
  background: linear-gradient(135deg, #6c57ff 0%, #5146ee 100%);
  color: #fff;
  box-shadow: 0 14rpx 30rpx rgba(83, 69, 238, .22);
}

.share-btn.poster {
  margin-top: 0;
  background: #fff;
  color: #5a4bf4;
  border: 1rpx solid #dedaff;
}

.share-btn::after {
  border: 0;
}

.invite-link {
  height: 72rpx;
  margin-top: 18rpx;
  border-radius: 18rpx;
  border: 2rpx dashed #bcb5ff;
  background: rgba(255, 255, 255, .78);
  color: #5a4bf4;
  font-size: var(--font-body);
  line-height: 72rpx;
  font-weight: var(--weight-bold);
}

.share-actions {
  display: flex;
  justify-content: center;
}

.share-btn.reward {
  width: 100%;
  height: 84rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, #765cff 0%, #5146ee 100%);
  box-shadow: 0 14rpx 30rpx rgba(83, 69, 238, .24);
  color: #fff;
  font-size: var(--font-lg);
  line-height: 84rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 18rpx;
  padding: 0 34rpx;
}

.share-btn.reward::before,
.share-btn.poster,
.share-btn.poster::before,
.invite-link,
.invite-link::before {
  display: none;
}

.share-weixin-icon {
  width: 46rpx;
  height: 46rpx;
  display: block;
  flex: 0 0 auto;
  border-radius: 10rpx;
}

.share-btn.reward text {
  display: block;
  line-height: 42rpx;
  white-space: nowrap;
}

.user-box {
  min-height: 154rpx;
  height: auto;
  margin: 6rpx 0 24rpx;
  padding: 26rpx 30rpx;
  border-radius: 28rpx;
  background:
    radial-gradient(circle at 10% 8%, rgba(112, 92, 255, .10), transparent 34%),
    linear-gradient(135deg, rgba(255, 255, 255, .96), rgba(250, 250, 255, .90));
  border: 1rpx solid #eceefa;
  box-shadow: 0 18rpx 44rpx rgba(44, 54, 88, .07);
  display: flex;
  align-items: center;
  box-sizing: border-box;
}

.guest-only {
  min-height: 154rpx;
  margin: 6rpx 0 24rpx;
  padding: 0 30rpx;
  border-radius: 28rpx;
  background: rgba(255, 255, 255, .96);
  border: 1rpx solid #eceefa;
  box-shadow: 0 18rpx 44rpx rgba(44, 54, 88, .07);
  color: #171923;
  font-size: var(--font-hero);
  line-height: 154rpx;
  font-weight: var(--weight-bold);
  box-sizing: border-box;
}

.avatar-wrap {
  flex: 0 0 auto;
  width: 118rpx;
  height: 118rpx;
  margin-right: 26rpx;
  border-radius: 50%;
  padding: 6rpx;
  background: linear-gradient(135deg, #ffffff, #eeeaff);
  box-shadow: 0 12rpx 28rpx rgba(94, 80, 190, .15);
  box-sizing: border-box;
}

.avatar {
  width: 100%;
  height: 100%;
  margin-right: 0;
  border: 0;
  border-radius: 50%;
  box-shadow: none;
  background: #e8e5f7;
}

.user-copy {
  flex: 1 1 auto;
  min-width: 0;
}

.user-topline {
  display: flex;
  align-items: center;
  gap: 14rpx;
  min-width: 0;
}

.nickname {
  flex: 0 1 auto;
  min-width: 0;
  color: #171923;
  font-size: var(--font-hero);
  line-height: 48rpx;
  font-weight: var(--weight-bold);
  letter-spacing: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.login-state {
  flex: 0 0 auto;
  height: 44rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  background: #f0edff;
  color: #5a4bf4;
  font-size: var(--font-xs);
  line-height: 44rpx;
  font-weight: var(--weight-bold);
}

.login-state.guest {
  background: #fff3e4;
  color: #c97813;
}

.login-state.logged {
  background: #eafbf1;
  color: #10a35a;
  border: 1rpx solid #bdeccf;
}

.profile-tip-row {
  min-width: 0;
  margin-top: 16rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.profile-tip {
  flex: 1 1 auto;
  min-width: 0;
  color: #526079;
  font-size: var(--font-sm);
  line-height: 36rpx;
  font-weight: var(--weight-semibold);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.login-action {
  flex: 0 0 auto;
  height: 42rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  background: #ffffff;
  border: 1rpx solid #e3e6f2;
  color: #5a4bf4;
  font-size: var(--font-sm);
  line-height: 42rpx;
  font-weight: var(--weight-bold);
}

.list-icon {
  flex: 0 0 auto;
  width: 70rpx;
  height: 70rpx;
  margin-right: 30rpx;
  border-radius: 0;
  background: transparent;
  box-shadow: none;
  display: block;
}

.list-icon.doc::after,
.list-icon.bag::after {
  display: none;
}

.line {
  margin-left: 100rpx;
}
